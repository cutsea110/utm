# TODO

## ターゲット TM のシンボル数制限を解除する

- [x] v1 の仮想テープセル形式を決め、`UTM.hs` に不変条件を記述する
  - `VC bits...` を通常セル、`HVC bits...` を仮想ヘッド付きセルとする
  - 記号コードはターゲット TM ごとの固定幅とする
- [x] `UTM.S` に `VC` / `HVC` を追加し、構造記号として書込み制約を定義する
- [x] `Codebook` の記号対応を `Symbol tm -> [UTM.S]` へ変更する
- [x] `symbolWidth` を実装する (`max 1 (ceil(log2 |Σ|))`)
- [x] D の `a` / `b` フィールドを固定幅ビット列 + `SP` 終端へ変更する
- [x] `WS` を固定幅ビット列 + 終端 `B` の作業領域へ変更する
- [x] `encodeVirtualTape` を `VC` / `HVC` + 固定幅ビット列のセル列へ変更する
- [x] 仮想テープの左右に空白セルのバッファを確保する
- [x] `copyCurrentHeadToWS` を、`HVC` セルのビット列を `WS` へコピーする実装へ変更する
- [x] `findTransition` の `a` と `WS` の照合を `matchUntil` によるビット列照合へ変更する
- [x] `writeVirtualSymbol` を、D の `b` ビット列で `HVC` セル本体を上書きする実装へ変更する
- [x] 仮想ヘッド移動を `HVC -> VC` / `VC -> HVC` のセル開始記号の置換として実装する
- [x] `decodeConfiguration` を v1 の仮想テープセル形式へ対応させる
- [x] 4記号以上を持つターゲット TM を追加し、`encode -> utm -> decodeConfiguration` の統合テストを書く
- [x] 既存の TM1 と doctest が v1 形式で通ることを確認する

## 仮想テープを左へ無限に拡張する

- [x] 左バッファを、`VT` と最初の `HVC` の間にある未使用の `B` ブロック列として定義する
  - `VT` 自体は移動・書換えしない
  - `HVC` より右の `B` は従来どおり仮想テープの右端終端、`VT` より右かつ最初の `HVC` より左の `B` は左バッファとする
- [ ] `VT` から `HVC` を探す処理が、左バッファの `B` ブロックを通過できるようにする
- [ ] 左バッファを使い切ったとき、既存セル列を右端へ丸ごと退避して左バッファを再確保する手順を設計・実装する
  - 拡張中だけ `HVC` を2個許可し、元列を走査する cursor と退避先先頭の sentinel として用いる
  - cursor のセルを右端へ `VC` としてコピーし、コピー後に元ブロックを `B` ブロックへ戻す
  - cursor の右隣を次の cursor にする。右隣が sentinel の `HVC` なら完了とする
  - sentinel は `HVC + blankCode` として残し、退避済みの元領域は新しい左バッファとする
- [ ] 左端超過時に拡張後の左バッファを1ブロック消費し、左移動を再試行する
- [ ] 左方向へ初期バッファ以上に移動する統合テストを書く
